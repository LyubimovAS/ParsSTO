ПАРСЕР СЕЛЕНИУМ

├─ main.py               # Главный скрипт парсера
├─ parser_service.py     # Доп. функции для парсинга (по необходимости)
├─ database.py           # Работа с базой данных SQLite
├─ setup_env.py          # Скрипт установки зависимостей и подготовки окружения
├─ requirements.txt      # Список Python-библиотек
├─ data/                 # Папка для базы данных
└─ venv/                 # Виртуальное окружение (если используется)

🧰 Используемые технологии

Python 3.11+

Selenium (автоматизация браузера)

SQLite (локальная база данных)

Webdriver Manager (авто-обновление драйвера Chrome)

Логирование через logging для удобного контроля

📝 Функции

Парсинг категорий услуг и цен с сайта

Очистка и форматирование цен

Сохранение данных в локальную базу SQLite

Логирование ошибок и прогресса

⚡ Примечания

Для корректной работы ChromeDriver должен быть совместим с версией Chrome.

В скрипте main.py можно включить режим headless, если не нужен визуальный контроль браузера:

options.add_argument("--headless")
